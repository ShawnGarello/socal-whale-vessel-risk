export default function Home() {
  return (
    <main>
      <h1>SoCal whale–vessel spatial overlap</h1>
      <p>
        Application foundation. No project layers are present yet — the map, the input
        layers, and the exposure results are added by later milestones.
      </p>
    </main>
  );
}
