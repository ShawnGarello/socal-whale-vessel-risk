// Registers the JSX intrinsic-element types for the ArcGIS map web components,
// so `<arcgis-map>` and friends are type-checked rather than treated as unknown
// elements. Required once per project; see the SDK's React setup guide.
/// <reference types="@arcgis/map-components/types/react" />
